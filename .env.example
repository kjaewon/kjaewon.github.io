# 은퇴 카운트다운 웹앱 — 환경 변수 예시
# 현재 버전은 서버/API 없이 LocalStorage만 사용하므로 필수 항목은 없습니다.
# Vercel 배포 시 이 파일을 복사해 .env.local 등으로 쓰지 않아도 동작합니다.
#
# 추후 백엔드/분석 등을 붙일 때 아래처럼 추가할 수 있습니다.
# Vite에서는 클라이언트에 노출할 변수만 VITE_ 접두사를 붙여야 합니다.

# VITE_APP_TITLE=은퇴 카운트다운
# VITE_API_BASE_URL=https://api.example.com
